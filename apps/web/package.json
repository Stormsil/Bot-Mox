{
  "name": "@botmox/web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --dir ../../bot-mox dev",
    "build": "pnpm --dir ../../bot-mox build",
    "lint": "pnpm --dir ../.. exec biome check bot-mox/src && pnpm --dir ../.. run check:ui:boundaries",
    "typecheck": "pnpm --dir ../../bot-mox exec tsc -b --pretty false",
    "contract:check": "pnpm --filter @botmox/api-contract contract:check",
    "test:e2e": "pnpm --dir ../../bot-mox test:e2e",
    "check": "pnpm run lint && pnpm run typecheck && pnpm run build"
  }
}
