#VY|- 2026-03-03: Tasks 1-6 complete. Keep stream flags fallback-safe by default.
#TT|- 2026-03-03: Contract/OpenAPI drift gate is wired via `scripts/check-contract-openapi-sync.js`.
#JB|- 2026-03-03: Parity harness lives in `apps/backend/src/modules/__tests__/business-logic-parity.*` and uses Task 1 fixtures.
#XS|- 2026-03-03: For schedule parity, seeded deterministic checks are required and already established in parity tests.
#QJ|- 2026-03-03: Task 9 finance aggregate endpoints return DTO-first data (`summary`, `breakdown`, `time-series`) with strict `from_ts<=to_ts` 400 envelope and server-side gap-filled day buckets for chart stability.
#NM|- 2026-03-03: Backend status mapping keeps frontend semantics by using strict banned precedence, 5-minute bot offline threshold, and default warning window of 7 days for expiring resource flags.
#KZ|- 2026-03-03: Task 7 typing fix used explicit `Record<string, unknown>` callback params in `.find(...)` to clear TS7006 with zero assertion/behavior changes.
#PT|- 2026-03-03: Task 8 frontend status cutover now reads backend DTO fields (`computed_status`, `days_remaining`, `is_expiring_soon`) in project selectors with `STATUS_BACKEND_AUTHORITATIVE` gating and local semantic fallback retained for rollback safety.
#JP|- 2026-03-03: Removed `apps/frontend/src/entities/bot/lib/statuses.ts`; status computations are now consumer-local where fallback is required (project selectors, datacenter stats helper), eliminating shared status utility imports.
#XK|- 2026-03-03: Task 10 finance cutover now reads `/finance/summary`, `/finance/breakdown`, and `/finance/time-series` aggregate DTOs behind `FINANCE_BACKEND_AUTHORITATIVE`, while retaining local fallback for rollback-safe rendering.
#WB|- 2026-03-03: Removed `apps/frontend/src/entities/finance/lib/analyticsCalculations.ts`; analytics helpers were inlined into `analytics.ts` and remaining imports were migrated.
#WK|- 2026-03-03: Task 10 TS fix: resolved `FinanceSummary` type/component naming collision in finance page by aliasing imports and adding explicit callback param typing for strict TS checks.
#MW|- 2026-03-03: Parity blocker fix: `apps/backend/src/modules/__tests__/business-logic-parity.test.ts` no longer imports removed `apps/frontend/src/entities/bot/lib/statuses.ts`; it now consumes `statuses.types.ts` (`OFFLINE_THRESHOLD_MS`) and keeps parity-only status adapters local to the test harness.
#JN|- 2026-03-03: Task 12 VM delete cutover now fetches backend evaluation DTOs from `POST /api/v1/vms/evaluate-deletion` in `vmDeleteContextFacade`, and `useDeleteVmWorkflow` renders backend-authoritative `can_delete`/`reason` for row locking and decision text.
#KW|- 2026-03-03: VM delete workflow path no longer imports `deleteVmWorkflowCandidates` or uses `evaluateDeleteBot`; filter/view toggles remain local UI concerns while deletion eligibility authority moved to backend payload.
#VN|- 2026-03-03: Task 12 acceptance fix extracted VM delete types into `model/deleteVm.types.ts`, removed `deleteVmRules.ts` and `deleteVmWorkflowCandidates.ts`, and rewired UI/model imports to keep zero remaining references to local delete rule modules.
#NK|- 2026-03-03: Task 13 implemented `POST /settings/schedule/generate` in backend settings module with deterministic seed mode (`seed` => reproducible output), random mode fallback, frontend-parity validation rules, and DTO payload including `days`, `allowedWindows`, `weekSchedule`, `items`, and deterministic metadata.
#YM|- 2026-03-03: Backend parity harness is resilient to frontend VM-delete module cleanup when rule/candidate adapter helpers live in-test; this keeps `vm-delete` parity deterministic without importing deleted frontend lib files.
#HN|#DG|- 2026-03-03: Task 15 backend VM patch migration added tenant-safe `POST /vms/patch/plan` and `POST /vms/patch/apply` in infra with backend patch semantics parity for MAC/serial/bridge/VNC/IP and deterministic seed handling.
#JX|- 2026-03-03: Task 16 frontend queue now calls backend patch plan/apply endpoints with one shared seed per VM item to keep plan/apply deterministic within the run.
#HK|#LF|- 2026-03-03: Task 17 cleanup removed frontend backend-authoritative runtime flag branching for status, finance, VM delete, and schedule streams; migrated flows now run backend-authoritative only.
#KM|#RA|- 2026-03-03: Deprecated frontend migration flag config fields were removed from runtime/env config after eliminating all active callsites.
#BM|- 2026-03-03: Task 17 corrective pass removed finance aggregate local fallback math in `apps/frontend/src/pages/finance/index.tsx`; summary, breakdown, and time-series now use backend aggregate DTOs only.
#TQ|- 2026-03-03: Task 17 corrective pass removed residual migration-flag artifacts (`docker-entrypoint` runtime keys, migration-check script/test, and active runbook/scripts references); grep for `*_BACKEND_AUTHORITATIVE` is now empty.
#XW|
#NR|- 2026-03-03: F1 compliance audit PASS after corrective Task 17 pass; verified zero active migration flags ( grep empty), removed frontend compute utility files absent/unreferenced, and full quality gate exits all 0 (, , ).
#JJ|
#NS|- 2026-03-03: F1 audit correction note: quality-gate evidence commands all exit 0 (`pnpm --filter @botmox/api-contract build`, `pnpm --filter @botmox/backend test`, `pnpm --filter @botmox/backend build`, `pnpm --filter @botmox/frontend exec tsc -b --pretty false`, `pnpm --filter @botmox/frontend build`); migration-flag grep remains empty.
#QR|- 2026-03-03: F3 QA rerun executed stream-level happy/failure checks for status, finance, vm-delete, schedule, and vm-patch queue via Playwright user flows (`authenticated-shell.spec.ts`) plus backend runtime tests (`bots.controller`, `bots.service`, `finance.controller`, `settings.controller`, `infra-vms.controller`).
#SP|- 2026-03-03: VM delete user flow confirms backend-authoritative lock messaging when decision payload is unavailable (`Rule: VM deletion decision unavailable from backend`), preventing permissive frontend fallback behavior.
#F4|- 2026-03-03: F4 scope-fidelity audit confirms structural migration refactor exists in source diff: frontend local business-logic utilities removed (`apps/frontend/src/entities/bot/lib/statuses.ts`, `apps/frontend/src/entities/finance/lib/analyticsCalculations.ts`, `apps/frontend/src/features/vm-management/lib/deleteVmRules.ts`, `apps/frontend/src/features/vm-management/lib/deleteVmWorkflowCandidates.ts`, `apps/frontend/src/shared/lib/utils/schedule/generation.ts`, `apps/frontend/src/shared/lib/utils/vm/patcher.ts`, `apps/frontend/src/shared/lib/utils/vm/index.ts`) and backend-authoritative endpoints/consumers are present (`apps/backend/src/modules/finance/finance.controller.ts`, `apps/backend/src/modules/settings/settings.controller.ts`, `apps/backend/src/modules/infra/infra.service.ts`, `apps/frontend/src/pages/finance/index.tsx`, `apps/frontend/src/entities/vm/api/vmDeleteContextFacade.ts`, `apps/frontend/src/features/vm-management/model/vm/queue/configureVmItem.ts`).
#F4|- 2026-03-03: F4 quality sanity checks passed in this workspace (`pnpm run check:contract:openapi-sync`, `pnpm --filter @botmox/api-contract build`, `pnpm --filter @botmox/backend build`, `pnpm --filter @botmox/frontend exec tsc -b --pretty false`) and `lsp_diagnostics` reported no diagnostics on sampled modified source files across backend/frontend/contracts.
#F4|- 2026-03-03: F4 scope classification: docs/scripts cleanup removing `migration:check*` callsites is required and aligns with Task 17 corrective wave; historical-only references remain in archived audit docs (`docs/history/**`, `docs/audits/**`) and are non-runtime.
#F4|- 2026-03-03: F4 fail condition: `packages/api-contract/tsconfig.json` (`declaration: false`) and `.sisyphus/boulder.json` plan-session metadata changes are not required by Tasks 1-17 or Task 17 corrective migration cleanup and must be separated/excluded for strict scope-fidelity pass.

31#F2|- 2026-03-03: F2 final-wave quality review PASS: migration-scope anti-pattern scans are clean, stale import checks to removed frontend utilities are clean, and required verification commands all exit 0.
- 2026-03-03: F3 blocker fix keeps migration VM-delete e2e backend-authoritative by asserting backend lock reason text () instead of legacy  expectation.
- 2026-03-03: F3 blocker fix learning: VM-delete migration e2e now asserts backend lock reason text Rule: VM deletion decision unavailable from backend instead of ALLOWED.

39#QF|#F4|- 2026-03-03: F4 blocker fix restored .sisyphus/boulder.json and packages/api-contract/tsconfig.json to HEAD-only state; strict scope hygiene avoids non-plan metadata or config drift in migration diff.

- 2026-03-03: TS7056 retry resolution for api-contract keeps explicit named  annotations on both definitions and contract exports; avoid  declaration signatures because they triggered TS4023/TS4058 unnameable-type emit errors.

- 2026-03-03: TS7056 retry resolution for api-contract keeps explicit named ApiContractDefinitions annotations on definitions and contract exports; avoid c-router ReturnType declaration signatures because they triggered TS4023/TS4058 unnameable-type emit errors.
