- Removed queue mirror sync from `useVmsPageViewModel` and switched queue reads to `useVmWorkspaceQueueItems`.
- Typecheck was broken because `useVMQueue` no longer returns `queue`, but `queue.queue` was still referenced.
- Delete workflow and start/queue actions now consume canonical queue items from Zustand selector output.
- Invariant for next tasks: keep `useVmWorkspaceStore.queue.items` as the only queue list source and avoid any mirror `useEffect` writes.
- Passing queue items as a dedicated `queueItems` input keeps action APIs stable while removing `queue.queue` wrapper reads.
- Live refs stay trustworthy when fed directly from `useVmWorkspaceQueueItems`, which avoids stale snapshots during queue edits.
- Converting `VMQueuePanelProps` to `{}` while accepting a temporary `Partial<LegacyProps>` keeps task 6 decoupling forward-compatible without changing `VMsPage` callsite in task 7.
- Publishing queue panel business actions through a bridge hook (`setVMQueuePanelBridgeValue` + `useVMQueuePanelBridgeValue`) avoids a second controller instance and keeps add/clear/delete/start/update behavior single-sourced.
- Row subtree contract remains stable when `VMQueueContextProvider` continues to receive the same context shape, even after panel ownership shifts from props to internal bridge/store reads.
- After converting `VMsPage` to `queuePane={<VMQueuePanel />}`, queue-pane decoupling is explicit at the page boundary and easy to guard with static JSX checks.
- Removing queue-panel-only fields from `useVmWorkspaceController` return surface prevents dead prop-drill outputs while preserving internal bridge wiring inside the controller.
- With `VMsPage` now rendering only `<VMQueuePanel />`, keeping a `Partial<LegacyProps>` fallback in the panel is dead compatibility surface and can be removed without behavior impact.
- `VmDeleteCandidate` in queue `types.ts` had zero consumers; deleting it and its `ProxmoxVM` import reduces stale queue-contract residue cleanly.
- Bridge-first reads in `VMQueuePanel` (`isProcessing`, start actions, metadata, row mutations) keep active queue behaviors intact while enforcing the page/widget boundary.
- Deterministic gate scripts are easiest to audit when each policy emits explicit `PASS`/`FAIL` lines plus a single `FINAL_EXIT_CODE` contract.
- A stale-snapshot simulation inside the checker reliably proves non-zero failure semantics without mutating `apps/frontend/src` implementation files.
- Bare-callsite validation for `VMQueuePanel` is safer when the check inspects matched JSX tags and enforces exactly one self-closing `<VMQueuePanel />` shape.
- Regression wave gate order (lint -> typecheck -> build) stayed green, which isolates remaining risk to e2e runtime/tooling conditions.
- Playwright VM/queue smoke run failed with a concrete blocker signature (localhost:4173 already used plus ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL Command "playwright" not found), so this should be tracked as environment instability rather than product behavior drift.
- Next rerun should first clear port 4173 occupancy and validate filtered-package Playwright availability, then repeat the exact VM/queue grep command for acceptance closure.
- Playwright config supports conflict-free reruns via E2E_FRONTEND_PORT, so port 4173 contention can be bypassed without killing unrelated local processes.
- In this repo the reliable VM/queue invocation is from apps/frontend context: pnpm exec playwright test --grep "VM|queue".
- Passing rerun on alternate port (2 tests passed) is sufficient to close task-10 only when lint, typecheck, and build evidence are already green in the same wave.
- Scope audits stay deterministic when they start from `git diff --name-only` and classify every touched file against explicit epic boundaries.
- Treating `.sisyphus/boulder.json` as orchestration metadata prevents false-positive scope-creep flags while keeping product-scope checks strict.
- Queue decoupling work remains scope-safe when each product diff can be mapped to either Zustand queue ownership migration or `<VMQueuePanel />` prop-drilling removal.
- F2 review confirms wrapper-residue checks are strongest when pairing bare-callsite validation in `VMsPage.tsx` with bridge-consumer checks in `VMQueuePanel.tsx`.
- Bridge coherence stays maintainable when ownership is explicit: one bridge producer in `useVmsPageViewModel` and one consumer hook in `VMQueuePanel`, plus unmount reset.
- Passing `queueItems` explicitly into queue-dependent hooks improves boundary hygiene and makes stale-data risks easier to audit than hidden wrapper reads.
- F3 UI spot-check is most reliable when the scenario asserts queue header primitives directly on `/vms`: panel title plus `+ VM`, `Start all`, and `Delete VM` entry points.
- Running Playwright with `E2E_FRONTEND_PORT=4273` avoids prior port contention while preserving canonical VM/queue command shape.
- Pairing a targeted VM queue visibility scenario with the repo-standard `--grep "VM|queue"` run gives stronger evidence for both UI affordance presence and regression safety.
