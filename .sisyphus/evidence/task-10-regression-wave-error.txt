Task 10 blocker evidence: Playwright VM|queue focused run

Command:
pnpm --filter @botmox/frontend exec playwright test --grep "VM|queue"

Observed failure signatures (exact):
1) Error: http://localhost:4173 is already used, make sure that nothing is running on the port/url or set reuseExistingServer:true in config.webServer.
2) ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL Command "playwright" not found

Classification:
- Blocker type: Environment/runtime instability outside deterministic app logic verification.
- Impact: Required focused VM/queue Playwright gate could not complete as a passing run.

Suggested next-step unblockers:
1) Free/stop process bound to http://localhost:4173 before rerun.
2) Verify Playwright binary availability in @botmox/frontend context (or use project canonical e2e script if package-scoped exec is unavailable).
3) Rerun exactly: pnpm --filter @botmox/frontend exec playwright test --grep "VM|queue"

Status:
- Required command remains non-zero in this regression wave.
