Task 12 - Error/Issue Log

Observed during required grep gate:
- Scoped command still reports HEX literals under `apps/frontend/src/widgets/bot-profile` in modules not migrated by Task 12:
  - `ui/license/*`
  - `ui/subscription/*`
  - `ui/proxy/*`
  - `ui/BotLogsWidget.tsx`
  - `ui/BotProxy.tsx`

Impact:
- Non-blocking for Task 12 objective.
- Domain bridge for WoW colors is implemented and wired for WoW widgets.

Typecheck:
- No TypeScript errors from Task 12 changes.

Resolution update (final pass):
- Previously reported residual HEX literals in bot-profile TS/TSX were migrated to semantic CSS variable usage.
- Current strict acceptance grep returns matches only in `apps/frontend/src/features/wow-data/config/colors.ts`.
- No active blockers remain for Task 12.
