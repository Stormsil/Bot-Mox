# IPQualityScore Proxy Server Configuration

# Server Configuration
PORT=3001

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173

# IPQualityScore API Key (fallback if Firebase is not configured)
IPQS_API_KEY=your_ipqs_api_key_here

# Firebase Configuration (optional - for reading API key from Firebase)
# Path to your Firebase service account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=../Assets/firebase-key.json

# Firebase Realtime Database URL
FIREBASE_DATABASE_URL=https://botfarm-d69b7-default-rtdb.europe-west1.firebasedatabase.app/

# Note: If Firebase is not configured, the server will use IPQS_API_KEY from .env
