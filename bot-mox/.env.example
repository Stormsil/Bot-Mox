# Frontend runtime
VITE_API_BASE_URL=http://localhost:3001
VITE_WS_BASE_URL=ws://localhost:3001

# Supabase Auth (email/password)
# Self-hosted local default: http://127.0.0.1:54321
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: local dev auth fallback token for internal-only mode
# If omitted in dev, frontend uses fallback: change-me-api-token
VITE_INTERNAL_API_TOKEN=change-me

# Dev auth behavior
# true  = bypass login screen in Vite dev mode (default behavior if variable is omitted)
# false = enforce real login even in dev
VITE_DEV_BYPASS_AUTH=true
