# Bot-Mox - Документация компонентов

> **Документация всех React компонентов**  
> Последнее обновление: 2026-01-30

---

## Содержание

1. [UI Компоненты](#ui-компоненты)
2. [Layout Компоненты](#layout-компоненты)
3. [Bot Компоненты](#bot-компоненты)
4. [Типы и Интерфейсы](#типы-и-интерфейсы)

---

## UI Компоненты

### StatusBadge

**Файл**: [`bot-mox/src/components/ui/StatusBadge.tsx`](bot-mox/src/components/ui/StatusBadge.tsx:1)

**Назначение**: Отображение статуса бота с цветовой индикацией.

**Пропсы**:

| Проп | Тип | Обязательный | Описание |
|------|-----|--------------|----------|
| `status` | `BotStatus` | Да | Статус бота |
| `showText` | `boolean` | Нет | Показывать текст (default: true) |
| `size` | `'default' \| 'small'` | Нет | Размер бейджа (default: 'default') |

**Поддерживаемые статусы**:

| Статус | Цвет | Текст |
|--------|------|-------|
| `offline` | #8c8c8c | OFFLINE |
| `prepare` | #1890ff | PREPARE |
| `leveling` | #722ed1 | LEVELING |
| `profession` | #eb2f96 | PROFESSION |
| `farming` | #52c41a | FARMING |
| `banned` | #f5222d | BANNED |

**Пример использования**:
```tsx
<StatusBadge status="farming" />
<StatusBadge status="offline" showText={false} size="small" />
```

**Статус**: ✅ Готов

---

### MetricCard

**Файл**: [`bot-mox/src/components/ui/MetricCard.tsx`](bot-mox/src/components/ui/MetricCard.tsx:1)

**Назначение**: Карточка метрики в стиле Proxmox.

**Пропсы**:

| Проп | Тип | Обязательный | Описание |
|------|-----|--------------|----------|
| `label` | `string` | Да | Заголовок метрики |
| `value` | `string \| number` | Да | Значение |
| `subtext` | `string` | Нет | Подпись под значением |
| `progress` | `number` | Нет | Процент для прогресс-бара |
| `icon` | `React.ReactNode` | Нет | Иконка |
| `color` | `string` | Нет | Цвет (default: '#e57000') |

**Пример использования**:
```tsx
<MetricCard
  label="Total Bots"
  value={18}
  subtext="80% online"
  progress={80}
  icon={<RobotOutlined />}
/>
```

**Статус**: ✅ Готов

---

### LoadingState

**Файл**: [`bot-mox/src/components/ui/LoadingState.tsx`](bot-mox/src/components/ui/LoadingState.tsx:1)

**Назначение**: Индикатор загрузки.

**Пропсы**:

| Проп | Тип | Обязательный | Описание |
|------|-----|--------------|----------|
| `message` | `string` | Нет | Сообщение при загрузке |

**Статус**: ✅ Готов

---

### ErrorBoundary

**Файл**: [`bot-mox/src/components/ui/ErrorBoundary.tsx`](bot-mox/src/components/ui/ErrorBoundary.tsx:1)

**Назначение**: Перехват ошибок React.

**Пропсы**:

| Проп | Тип | Обязательный | Описание |
|------|-----|--------------|----------|
| `children` | `React.ReactNode` | Да | Дочерние компоненты |
| `fallback` | `React.ReactNode` | Нет | UI при ошибке |

**Статус**: ✅ Готов

---

## Layout Компоненты

### Header

**Файл**: [`bot-mox/src/components/layout/Header.tsx`](bot-mox/src/components/layout/Header.tsx:1)

**Назначение**: Верхняя панель с логотипом и меню пользователя.

**Пропсы**: Нет

**Функциональность**:
- Отображение логотипа Bot-Mox
- Меню пользователя (dropdown)
- Кнопка выхода

**Интеграции**:
- [`useGetIdentity`](bot-mox/src/components/layout/Header.tsx:11) - получение данных пользователя
- [`useLogout`](bot-mox/src/components/layout/Header.tsx:12) - выход из системы

**Статус**: ✅ Готов (требует подключения реальной аутентификации)

---

### ResourceTree

**Файл**: [`bot-mox/src/components/layout/ResourceTree.tsx`](bot-mox/src/components/layout/ResourceTree.tsx:1)

**Назначение**: Дерево ресурсов в стиле Proxmox (левая панель).

**Пропсы**: Нет

**Функциональность**:
- Иерархия: Datacenter → Projects → Status Groups → Bots
- Цветовая индикация статусов
- Навигация по клику
- Фильтрация по статусам (TODO)

**Типы узлов**:

| Тип | Описание |
|-----|----------|
| `datacenter` | Корневой узел |
| `project` | Игровой проект (WoW TBC, WoW Midnight) |
| `status_group` | Группировка по статусу |
| `bot` | Отдельный бот |
| `metrics` | Раздел метрик |
| `finance` | Раздел финансов |
| `licenses` | Управление лицензиями |
| `proxies` | Управление прокси |
| `subscriptions` | Управление подписками |

**Статусы ботов**:

| Статус | Цвет | Иконка |
|--------|------|--------|
| `offline` | #8c8c8c | PoweroffOutlined |
| `prepare` | #1890ff | ClockCircleOutlined |
| `leveling` | #722ed1 | RiseOutlined |
| `profession` | #eb2f96 | ToolOutlined |
| `farming` | #52c41a | PlayCircleOutlined |
| `banned` | #f5222d | StopOutlined |

**Статус**: ⚠️ В работе (использует моковые данные)

---

### ContentPanel

**Файл**: [`bot-mox/src/components/layout/ContentPanel.tsx`](bot-mox/src/components/layout/ContentPanel.tsx:1)

**Назначение**: Панель содержимого с табами.

**Пропсы**:

| Проп | Тип | Обязательный | Описание |
|------|-----|--------------|----------|
| `activeTab` | `TabType` | Нет | Активная вкладка |
| `onTabChange` | `(tab: TabType) => void` | Нет | Callback смены таба |
| `children` | `React.ReactNode` | Нет | Содержимое |
| `type` | `'bot' \| 'datacenter' \| 'project' \| 'metrics' \| 'finance'` | Нет | Тип панели |

**Типы табов (TabType)**:
```typescript
type TabType = 
  | 'summary' 
  | 'schedule' 
  | 'character' 
  | 'farm' 
  | 'leveling' 
  | 'profession' 
  | 'account' 
  | 'person' 
  | 'logs' 
  | 'license' 
  | 'proxy' 
  | 'subscription';
```

**Статус**: ⚠️ В работе (проблема с передачей children)

---

## Bot Компоненты

### BotSummary

**Файл**: [`bot-mox/src/components/bot/BotSummary.tsx`](bot-mox/src/components/bot/BotSummary.tsx:1)

**Назначение**: Сводная информация о боте и его статусе.

**Пропсы**:

| Проп | Тип | Описание |
|------|-----|----------|
| `bot` | `Bot` | Данные бота |

**Функциональность**:
- Проверка статуса лицензии (истекла/истекает)
- Проверка статуса прокси
- Проверка подписок
- Определение offline статуса
- Отображение предупреждений

**Статус**: ⚠️ В работе (нужно подключить реальные данные)

---

### BotCharacter

**Файл**: [`bot-mox/src/components/bot/BotCharacter.tsx`](bot-mox/src/components/bot/BotCharacter.tsx:1)

**Назначение**: Информация о персонаже бота.

**Пропсы**:

| Проп | Тип | Описание |
|------|-----|----------|
| `bot` | `Bot` | Данные бота |

**Функциональность**:
- Аватар персонажа
- Имя, уровень, раса, класс
- Сервер
- Инвентарь (моковые данные)

**Статус**: ⚠️ В работе (инвентарь моковый)

---

### BotLeveling

**Файл**: [`bot-mox/src/components/bot/BotLeveling.tsx`](bot-mox/src/components/bot/BotLeveling.tsx:1)

**Назначение**: Прогресс прокачки уровня.

**Пропсы**:

| Проп | Тип | Описание |
|------|-----|----------|
| `bot` | `Bot` | Данные бота |

**Функциональность**:
- Текущий уровень
- XP прогресс-бар
- XP в час
- Время до следующего уровня
- Текущая локация

**Статус**: ⚠️ В работе (моковые данные)

---

### BotProfession

**Файл**: [`bot-mox/src/components/bot/BotProfession.tsx`](bot-mox/src/components/bot/BotProfession.tsx:1)

**Назначение**: Прогресс профессий.

**Пропсы**:

| Проп | Тип | Описание |
|------|-----|----------|
| `bot` | `Bot` | Данные бота |

**Функциональность**:
- Список профессий
- Прогресс-бар для каждой
- Иконки профессий
- Статус активности

**Поддерживаемые профессии**:
- Mining (коричневый)
- Herbalism (зелёный)
- Skinning (оранжевый)
- Engineering (синий)

**Статус**: ⚠️ В работе (моковые данные)

---

### BotSchedule

**Файл**: [`bot-mox/src/components/bot/BotSchedule.tsx`](bot-mox/src/components/bot/BotSchedule.tsx:1)

**Назначение**: Расписание работы бота.

**Пропсы**:

| Проп | Тип | Описание |
|------|-----|----------|
| `bot` | `Bot` | Данные бота |

**Функциональность**:
- Таблица расписания по дням недели
- Время начала/окончания
- Включение/выключение слотов
- Добавление/удаление слотов

**🐛 Известный баг**: Отсутствует импорт `dayjs`

**Статус**: ⚠️ В работе (нужен фикс импорта)

---

### BotFarm

**Файл**: [`bot-mox/src/components/bot/BotFarm.tsx`](bot-mox/src/components/bot/BotFarm.tsx:1)

**Назначение**: Статистика фарма.

**Пропсы**:

| Проп | Тип | Описание |
|------|-----|----------|
| `bot` | `Bot` | Данные бота |

**Функциональность**:
- Общее количество золота
- Золота в час
- Время сессии
- Инвентарь с фарма

**Статус**: ⚠️ В работе (моковые данные)

---

### BotAccount

**Файл**: [`bot-mox/src/components/bot/BotAccount.tsx`](bot-mox/src/components/bot/BotAccount.tsx:1)

**Назначение**: Данные игрового аккаунта.

**Пропсы**:

| Проп | Тип | Описание |
|------|-----|----------|
| `bot` | `Bot` | Данные бота |

**Функциональность**:
- Email аккаунта
- Пароль (скрыт/показан)
- Дата регистрации
- Копирование в буфер обмена

**Статус**: ⚠️ В работе (моковые данные)

---

### BotPerson

**Файл**: [`bot-mox/src/components/bot/BotPerson.tsx`](bot-mox/src/components/bot/BotPerson.tsx:1)

**Назначение**: Данные персоны (фейковые данные владельца).

**Пропсы**:

| Проп | Тип | Описание |
|------|-----|----------|
| `bot` | `Bot` | Данные бота |

**Функциональность**:
- Имя, фамилия
- Дата рождения
- Страна, город
- ZIP код

**Статус**: ⚠️ В работе (моковые данные)

---

### BotLicense

**Файл**: [`bot-mox/src/components/bot/BotLicense.tsx`](bot-mox/src/components/bot/BotLicense.tsx:1)

**Назначение**: Управление лицензией бота.

**Пропсы**:

| Проп | Тип | Описание |
|------|-----|----------|
| `bot` | `Bot` | Данные бота |

**Функциональность**:
- Отображение ключа лицензии
- Статус (активна/истекла/истекает)
- Дата истечения
- Дней осталось
- Копирование ключа

**Интеграции**: Firebase (`bot_licenses`)

**Статус**: ⚠️ В работе (подключен Firebase, но данные моковые)

---

### BotProxy

**Файл**: [`bot-mox/src/components/bot/BotProxy.tsx`](bot-mox/src/components/bot/BotProxy.tsx:1)

**Назначение**: Информация о прокси бота.

**Пропсы**:

| Проп | Тип | Описание |
|------|-----|----------|
| `bot` | `Bot` | Данные бота |

**Функциональность**:
- IP, порт, логин, пароль
- Провайдер прокси
- Страна
- Fraud score
- Статус (активен/истек/забанен)
- Дней осталось

**Интеграции**: Firebase (`proxies`, `bots/{id}/proxy`)

**Статус**: ⚠️ В работе (подключен Firebase, но данные моковые)

---

### BotSubscription

**Файл**: [`bot-mox/src/components/bot/BotSubscription.tsx`](bot-mox/src/components/bot/BotSubscription.tsx:1)

**Назначение**: Управление подписками бота.

**Пропсы**:

| Проп | Тип | Описание |
|------|-----|----------|
| `bot` | `Bot` | Данные бота |

**Функциональность**:
- Список подписок
- Тип подписки
- Статус (активна/истекла)
- Дата истечения
- Автопродление
- Дней осталось

**Интеграции**: Firebase (`subscriptions`)

**Статус**: ⚠️ В работе (подключен Firebase, но данные моковые)

---

### BotLogs

**Файл**: [`bot-mox/src/components/bot/BotLogs.tsx`](bot-mox/src/components/bot/BotLogs.tsx:1)

**Назначение**: Логи событий бота.

**Пропсы**:

| Проп | Тип | Описание |
|------|-----|----------|
| `bot` | `Bot` | Данные бота |

**Функциональность**:
- Список важных событий
- Фильтрация по типу
- Экспорт логов
- Цветовая индикация типов

**Типы событий**:

| Тип | Цвет | Описание |
|-----|------|----------|
| `ban` | #f5222d | Бан аккаунта |
| `level_up` | #52c41a | Повышение уровня |
| `death` | #fa8c16 | Смерть персонажа |

**Статус**: ⚠️ В работе (моковые данные)

---

### BotFinance

**Файл**: [`bot-mox/src/components/bot/BotFinance.tsx`](bot-mox/src/components/bot/BotFinance.tsx:1)

**Назначение**: Финансовая статистика бота.

**Пропсы**:

| Проп | Тип | Описание |
|------|-----|----------|
| `bot` | `Bot` | Данные бота |

**Функциональность**:
- Доходы и расходы
- Таблица транзакций
- Фильтрация по типу
- Экспорт данных

**Статус**: ⚠️ В работе (моковые данные)

---

## Типы и Интерфейсы

### Основные типы

**Файл**: [`bot-mox/src/types/index.ts`](bot-mox/src/types/index.ts:1)

```typescript
// Bot Types
interface Bot {
  id: string;
  name: string;
  project_id: 'wow_tbc' | 'wow_midnight';
  status: BotStatus;
  character: Character;
  last_seen: number;
}

type BotStatus = 'offline' | 'prepare' | 'leveling' | 'profession' | 'farming' | 'banned';

interface Character {
  name: string;
  level: number;
  class: string;
  race: string;
  server: string;
  inventory?: InventoryItem[];
}

interface InventoryItem {
  id: string;
  name: string;
  quantity: number;
  quality: 'common' | 'uncommon' | 'rare' | 'epic';
}

// Account Types
interface AccountData {
  email: string;
  password: string;
  registration_date: number;
  last_login?: number;
}

// Person Types
interface PersonData {
  first_name: string;
  last_name: string;
  birth_date: string;
  city: string;
  country: string;
  zip: string;
}

// Leveling Types
interface LevelingProgress {
  current_level: number;
  current_xp: number;
  max_xp: number;
  xp_per_hour: number;
  estimated_time_to_level: number;
  location: string;
}

// Profession Types
interface ProfessionProgress {
  name: string;
  level: number;
  max_level: number;
  skill_points: number;
  max_skill_points: number;
}

// Schedule Types
interface ScheduleEntry {
  id: string;
  day: 0 | 1 | 2 | 3 | 4 | 5 | 6;
  start_time: string;
  end_time: string;
  enabled: boolean;
}

// Note Types
interface Note {
  id: string;
  text: string;
  completed: boolean;
  created_at: number;
}

// Log Types
type LogEventType = 'ban' | 'level_up' | 'death';

interface LogEntry {
  id: string;
  bot_id: string;
  type: LogEventType;
  message: string;
  timestamp: number;
  details?: Record<string, unknown>;
}

// Project Types
interface Project {
  id: 'wow_tbc' | 'wow_midnight';
  name: string;
  game: string;
  max_level: number;
}

// Dashboard Types
interface DashboardMetrics {
  totalBots: number;
  activeBots: number;
  levelingBots: number;
  farmingBots: number;
  bannedBots: number;
}

// User Types
interface User {
  id: string;
  email: string;
  role: 'admin' | 'operator';
}

// Finance Types
interface FinanceEntry {
  id: string;
  type: 'income' | 'expense';
  category: 'subscription' | 'game' | 'proxy' | 'sale';
  amount: number;
  description: string;
  timestamp: number;
}

// License Types
interface BotLicense {
  id: string;
  key: string;
  type: 'sin' | 'other';
  status: 'active' | 'expired' | 'revoked';
  bot_id: string | null;
  expires_at: number;
  created_at: number;
  updated_at: number;
}

// Proxy Types
interface Proxy {
  id: string;
  ip: string;
  port: number;
  login: string;
  password: string;
  provider: string;
  country: string;
  type: 'http' | 'socks5';
  status: 'active' | 'expired' | 'banned';
  bot_id: string | null;
  fraud_score: number;
  expires_at: number;
  created_at: number;
  updated_at: number;
}

// Subscription Types
interface Subscription {
  id: string;
  bot_id: string;
  type: string;
  status: 'active' | 'expired' | 'cancelled';
  expires_at: number;
  auto_renew: boolean;
  created_at: number;
  updated_at: number;
}
```

---

## Сводка статусов

| Компонент | Статус | Примечание |
|-----------|--------|------------|
| StatusBadge | ✅ Готов | Полностью функционален |
| MetricCard | ✅ Готов | Полностью функционален |
| LoadingState | ✅ Готов | Полностью функционален |
| ErrorBoundary | ✅ Готов | Полностью функционален |
| Header | ⚠️ В работе | Нужна реальная аутентификация |
| ResourceTree | ⚠️ В работе | Моковые данные |
| ContentPanel | ⚠️ В работе | Проблема с children |
| BotSummary | ⚠️ В работе | Моковые данные |
| BotCharacter | ⚠️ В работе | Моковые данные |
| BotLeveling | ⚠️ В работе | Моковые данные |
| BotProfession | ⚠️ В работе | Моковые данные |
| BotSchedule | ⚠️ В работе | Нужен фикс dayjs |
| BotFarm | ⚠️ В работе | Моковые данные |
| BotAccount | ⚠️ В работе | Моковые данные |
| BotPerson | ⚠️ В работе | Моковые данные |
| BotLicense | ⚠️ В работе | Подключен Firebase |
| BotProxy | ⚠️ В работе | Подключен Firebase |
| BotSubscription | ⚠️ В работе | Подключен Firebase |
| BotLogs | ⚠️ В работе | Моковые данные |
| BotFinance | ⚠️ В работе | Моковые данные |

---

**Легенда**:
- ✅ Готов - Компонент полностью функционален
- ⚠️ В работе - Компонент работает, но требует доработки
- ❌ Не готов - Компонент не реализован
